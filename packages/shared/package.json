{
  "name": "@zephyr-deploy/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared constants, types and utilities across host and remote apps",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "typescript": "^5.7.0"
  }
}
